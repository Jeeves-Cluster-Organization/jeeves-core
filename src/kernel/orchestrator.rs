//! Pipeline orchestration and routing.

/// Orchestrator (checkpoint 6 - stub for now).
#[derive(Debug)]
pub struct Orchestrator {
    // Routing logic will be implemented in checkpoint 6
}

impl Orchestrator {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for Orchestrator {
    fn default() -> Self {
        Self::new()
    }
}
